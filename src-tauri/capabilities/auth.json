{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "auth",
  "description": "Capability for the auth login WebView",
  "windows": ["auth"],
  "permissions": [
    "core:default"
  ]
}

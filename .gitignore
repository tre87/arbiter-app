# Rust build output (covers the root crate + the vendored portable-pty/iced_winit)
target/

# Env files
.env
.env.local
.env.*.local

# Claude Code: CLAUDE.md is committed (shared project instructions) — `!` overrides the
# user's global gitignore, which ignores CLAUDE.md everywhere. `.claude/` stays local.
!CLAUDE.md
.claude/

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Logs
*.log
